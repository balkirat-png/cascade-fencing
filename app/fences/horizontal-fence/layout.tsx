import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Horizontal Fence Installation in Sumner, WA | Cascade Fencing",
  description: "Modern horizontal cedar fence installation with contemporary clean lines. Expert installation in Pierce County. Free estimates. Call (253) 228-9101.",
  keywords: "horizontal fence installation, modern horizontal fence, contemporary wood fence, horizontal cedar fence, modern fence Sumner WA",
};

export default function HorizontalFenceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
